# shinymapjp (Python)

Python asset package that will bundle Japan-prefecture geometry and helper utilities for the core `shinymap` Shiny bindings.

This is currently a placeholder awaiting migration from the legacy code.
