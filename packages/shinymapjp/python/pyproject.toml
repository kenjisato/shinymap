[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "shinymapjp"
version = "0.0.1"
description = "Japan-prefecture assets for shinymap."
readme = "README.md"
authors = [{name = "shinymap contributors"}]
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = [
  "shinymap>=0.0.1"
]

[project.urls]
Homepage = "https://github.com/kenjisato/shinymap"
